import React from "react";

const RouteContainer = (props: any) => (
  <div
    className="w-full flex flex-col items-center justify-center space-y-2 top-0 bottom-0 absolute"
    {...props}
  />
);
export default RouteContainer;
